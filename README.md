# GitHub Pages Repo

Minimal placeholder repo for a future `github.io` site.

Files to start with:
- `index.html`: landing page

Next steps:
1. Rename the GitHub repository to `<your-username>.github.io`.
2. Push this repo to GitHub.
3. Replace `index.html` with the real site content.
